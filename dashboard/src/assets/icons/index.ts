// Brands
import {
    cibDocker
} from '@coreui/icons'

// Linear
import {
    cilBullhorn,
    cilCheckCircle,
    cilCode,
    cilGraph,
    cilImage1,
    cilNotes,
    cilSettings,
    cilSpeedometer,
    cilSquare,
    cilThumbDown,
    cilThumbUp,
} from '@coreui/icons'

// Flags
import {
    cifUs,
    cifNo,
} from '@coreui/icons'

export const icons = {
    // Brands
    ...{
        cibDocker,
    },

    // Linear
    ...{
        cilBullhorn,
        cilCheckCircle,
        cilCode,
        cilGraph,
        cilImage1,
        cilNotes,
        cilSettings,
        cilSpeedometer,
        cilSquare,
        cilThumbDown,
        cilThumbUp,
    },

    // Flags
    ...{
        cifUs,
        cifNo,
    }
}
