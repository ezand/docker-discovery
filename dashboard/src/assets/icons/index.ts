// Brands
import {
    cibDocker
} from '@coreui/icons'

// Linear
import {
    cilGraph,
    cilSettings,
    cilSpeedometer
} from '@coreui/icons'

// Flags
import {
    cifUs,
    cifNo
} from '@coreui/icons'

export const icons = {
    // Brands
    ...{
        cibDocker
    },

    // Linear
    ...{
        cilGraph,
        cilSettings,
        cilSpeedometer
    },

    // Flags
    ...{
        cifUs,
        cifNo
    }
}
