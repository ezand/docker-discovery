// Brands
import {
    cibDocker
} from '@coreui/icons'

// Linear
import {
    cilCode,
    cilGraph,
    cilSettings,
    cilSpeedometer
} from '@coreui/icons'

// Flags
import {
    cifUs,
    cifNo
} from '@coreui/icons'

export const icons = {
    // Brands
    ...{
        cibDocker
    },

    // Linear
    ...{
        cilCode,
        cilGraph,
        cilSettings,
        cilSpeedometer
    },

    // Flags
    ...{
        cifUs,
        cifNo
    }
}
