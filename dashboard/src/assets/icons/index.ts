// Brands
import {
    cibDocker
} from '@coreui/icons'

// Linear
import {
    cilSpeedometer
} from '@coreui/icons'

// Flags
import {
    cifUs,
    cifNo
} from '@coreui/icons'

export const icons = {
    // Brands
    ...{
        cibDocker
    },

    // Linear
    ...{
        cilSpeedometer
    },

    // Flags
    ...{
        cifUs,
        cifNo
    }
}
